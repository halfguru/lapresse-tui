---
phase: 99-refactor
plan: 03
type: execute
wave: 3
depends_on: ["99-01"]
files_modified:
  - src/sync.rs
  - src/sync/mod.rs
  - src/sync/scraping.rs
  - src/sync/download.rs
  - src/sync/progress.rs
autonomous: true
requirements: [REFACTOR-04]

must_haves:
  truths:
    - "sync.rs decomposed into sync/ module directory with mod.rs, scraping.rs, download.rs, progress.rs"
    - "External API surface unchanged — crate::sync::* still resolves correctly via pub use re-exports"
    - "All 13 tests pass"
    - "cargo clippy passes clean"
  artifacts:
    - path: "src/sync/mod.rs"
      provides: "pub use re-exports preserving external API"
      exports: ["run_sync", "SyncStats", "sync_single_day_with_progress", "parse_day_page", "parse_article_page"]
    - path: "src/sync/scraping.rs"
      provides: "parse_day_page, parse_article_page, ArticleLink, ParsedArticle, ImageLink"
      min_lines: 200
    - path: "src/sync/download.rs"
      provides: "fetch_page, fetch_image, fetch_and_store_image"
      min_lines: 100
    - path: "src/sync/progress.rs"
      provides: "SyncStats, sync_single_day_with_progress"
      min_lines: 50
  key_links:
    - from: "src/sync/mod.rs"
      to: "src/sync/scraping.rs"
      via: "pub use scraping::parse_day_page"
      pattern: "pub use scraping::"
    - from: "src/main.rs"
      to: "src/sync/mod.rs"
      via: "crate::sync::* (unchanged by re-exports)"
      pattern: "crate::sync::"
---

<objective>
Decompose sync.rs (733 lines) into a sync/ module directory.

Purpose: Split the monolithic sync file into focused submodules: scraping (HTML parsing), download (HTTP fetching), and progress (types/stats). This is the cleanest decomposition target — sync.rs has the clearest internal boundaries.

Output: `src/sync.rs` deleted, replaced by `src/sync/mod.rs`, `src/sync/scraping.rs`, `src/sync/download.rs`, `src/sync/progress.rs`. All external imports still work via `pub use` re-exports.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/REFACTOR.md

@src/sync.rs
@src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sync module directory with submodules</name>
  <files>src/sync/mod.rs, src/sync/scraping.rs, src/sync/download.rs, src/sync/progress.rs</files>
  <action>
Decompose `src/sync.rs` into `src/sync/` module directory. The file currently has these clear regions:

**Line mapping (approximate):**
- Lines 1-42: Imports, SyncStats struct → split across submodules
- Lines 44-300: `run_sync()` — the CLI sync runner, uses scraping + downloading
- Lines ~150-175: `sync_single_day_with_progress()` — per-day sync orchestration
- Lines ~310-367: `ArticleLink`, `ParsedArticle`, `ImageLink` structs
- Lines 368-500: `parse_day_page()` — HTML parsing for day index
- Lines 498-620: `parse_article_page()` — HTML parsing for article content
- Lines ~620-700: `fetch_page()`, `fetch_image()`, `fetch_and_store_image()` — HTTP helpers
- Lines 701-733: (already deleted in Plan 01 — _for_test wrappers)

**Step-by-step:**

1. Create directory `src/sync/`

2. Create `src/sync/progress.rs` — move into it:
   - `SyncStats` struct and `impl` (with `#[derive(Default)]` from Plan 02)
   - Any sync-related type definitions needed by multiple submodules

3. Create `src/sync/scraping.rs` — move into it:
   - `ArticleLink` struct (make `pub(crate)`)
   - `ParsedArticle` struct (make `pub(crate)`)
   - `ImageLink` struct (make `pub(crate)`)
   - `parse_day_page()` function (already `pub(crate)` from Plan 01)
   - `parse_article_page()` function (already `pub(crate)` from Plan 01)
   - Required imports: `anyhow::Result`, `scraper::{Html, Selector}`, `url::Url`

4. Create `src/sync/download.rs` — move into it:
   - `fetch_page()` function (make `pub(crate)`)
   - `fetch_image()` function (make `pub(crate)`)
   - `fetch_and_store_image()` function (make `pub(super)` or `pub(crate)`)
   - Required imports: `anyhow::{Context, Result}`, `reqwest`, `crate::db::Db`, `std::sync::Arc`

5. Create `src/sync/mod.rs` — the public API surface:
   ```
   mod download;
   mod progress;
   mod scraping;
   
   pub use progress::SyncStats;
   pub use scraping::{parse_article_page, parse_day_page, ArticleLink, ParsedArticle};
   pub(crate) use download::{fetch_page, fetch_image, fetch_and_store_image};
   
   // Re-export types from app that sync uses
   use crate::app::{SyncMsg, SyncPhase, SyncPhaseKind};
   use crate::db::{Db, NewArticle, NewImage};
   ```
   - Keep `run_sync()` and `sync_single_day_with_progress()` in `mod.rs` since they orchestrate across submodules
   - OR move them into a `runner.rs` — but `mod.rs` is fine for the orchestration functions

6. Delete `src/sync.rs`

7. **Verify no import breakage**: The key consumers are:
   - `src/main.rs`: uses `crate::sync::run_sync` — re-exported ✓, uses `crate::sync::parse_day_page` — re-exported ✓
   - `src/app.rs`: uses `crate::sync::SyncMsg` — wait, `SyncMsg` is defined in `app.rs`, not `sync.rs`. `sync.rs` imports it FROM `app.rs`. So no change needed on `app.rs` side.

**Critical: No circular dependencies.** The dependency direction is:
- `sync/*` → `app` (for SyncMsg, SyncPhase types) — WRONG DIRECTION
- Actually: `sync.rs` imports `crate::app::{SyncMsg, SyncPhase, SyncPhaseKind}`. These are sync-related types that happen to live in `app.rs`.
- For now, keep these types in `app.rs` and import them. The `app.rs` decomposition (Plan 06) may move them later.

**Preserve all imports in other files:**
- `src/main.rs` line `crate::sync::run_sync` — works via re-export
- `src/main.rs` tests use `crate::sync::parse_day_page` — works via re-export
- `src/app.rs` does NOT import from sync — it defines the types sync uses
  </action>
  <verify>
    <automated>cd /home/simon/coding/lapresse-tui && cargo test 2>&1 && cargo clippy --all-targets -- -D warnings 2>&1</automated>
  </verify>
  <done>
- `src/sync.rs` deleted
- `src/sync/mod.rs` created with `pub use` re-exports
- `src/sync/scraping.rs` contains parse functions and data structs
- `src/sync/download.rs` contains HTTP fetch functions
- `src/sync/progress.rs` contains SyncStats
- `run_sync()` lives in mod.rs (orchestration)
- All 13 tests pass — `crate::sync::*` paths unchanged
- Clippy clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Internal module reorganization — no trust boundary changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-03 | N/A | N/A | accept | Module decomposition has no security implications |
</threat_model>

<verification>
1. `cargo test` — all 13 tests pass
2. `cargo clippy --all-targets -- -D warnings` — zero warnings
3. `cargo fmt --all -- --check` — formatted
4. `test -f src/sync.rs && echo "FAIL: old file still exists" || echo "PASS: old file removed"`
5. `test -f src/sync/mod.rs && echo "PASS: mod.rs exists" || echo "FAIL: mod.rs missing"`
6. `rg 'pub use' src/sync/mod.rs` — re-exports present
</verification>

<success_criteria>
- `sync.rs` decomposed into `sync/` module with 4 files
- External API unchanged via `pub use` re-exports
- All 13 tests pass
- Clippy and fmt clean
</success_criteria>

<output>
After completion, create `.planning/phases/99-refactor/99-03-SUMMARY.md`
</output>
