---
phase: 99-refactor
plan: 02
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - src/sync.rs
autonomous: true
requirements: [REFACTOR-03]

must_haves:
  truths:
    - "SyncStats derives Default and can be constructed with SyncStats::default()"
    - "All 13 tests pass after Default derive changes"
    - "cargo clippy passes clean"
  artifacts:
    - path: "src/sync.rs"
      provides: "SyncStats with #[derive(Default)]"
  key_links:
    - from: "sync.rs SyncStats"
      to: "callers"
      via: "SyncStats::default() or struct literal with ..Default::default()"
      pattern: "SyncStats::(default|new)\\(\\)"
---

<objective>
Add idiomatic Default derives to data structs.

Purpose: Replace manual `new()` constructors with derived `Default` where all fields have natural zero/empty values. This is idiomatic Rust and reduces boilerplate.

Output: `SyncStats` derives `Default`, `new()` kept as alias. All 13 tests pass.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/REFACTOR.md

@src/sync.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive Default on SyncStats</name>
  <files>src/sync.rs</files>
  <behavior>
    - SyncStats derives Default
    - SyncStats::default() produces identical result to SyncStats::new()
    - All 13 tests pass
    - Clippy clean
  </behavior>
  <action>
In `src/sync.rs`:

1. Add `#[derive(Default)]` to `SyncStats` struct (currently line ~22). All fields are `u32` which default to `0` — correct.
2. Keep `impl SyncStats { pub fn new() -> Self }` as-is — it's used in `run_sync()`. With `Default` derived, `new()` is semantically equivalent to `default()`. Optionally simplify `new()` body to `Self::default()` but this is cosmetic.
3. Run clippy to check for any warnings about redundant `new()` vs `Default` — if clippy suggests removing `new()`, remove it and update the single call site (`run_sync()`) to use `SyncStats::default()`.

Note: The research also mentions `SyncPhase` for Default. However `SyncPhase` has a `phase: SyncPhaseKind` field which has no natural default (it's an enum without a clear "zero" variant). Skip `SyncPhase` — only `SyncStats` gets Default.
  </action>
  <verify>
    <automated>cd /home/simon/coding/lapresse-tui && cargo test 2>&1 && cargo clippy --all-targets -- -D warnings 2>&1</automated>
  </verify>
  <done>
- `SyncStats` has `#[derive(Default)]`
- All 13 tests pass
- Clippy clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure internal refactoring — no trust boundary changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-02 | N/A | N/A | accept | Derive changes have no security implications |
</threat_model>

<verification>
1. `cargo test` — all 13 tests pass
2. `cargo clippy --all-targets -- -D warnings` — zero warnings
3. `cargo fmt --all -- --check` — formatted
4. `rg 'derive.*Default' src/sync.rs` — shows SyncStats derives Default
</verification>

<success_criteria>
- `SyncStats` derives `Default`
- All 13 tests pass
- Clippy and fmt clean
</success_criteria>

<output>
After completion, create `.planning/phases/99-refactor/99-02-SUMMARY.md`
</output>
