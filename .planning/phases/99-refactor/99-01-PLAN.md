---
phase: 99-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app.rs
  - src/db.rs
  - src/sync.rs
  - src/ui.rs
  - src/main.rs
autonomous: true
requirements: [REFACTOR-01, REFACTOR-02]

must_haves:
  truths:
    - "Zero #[allow(dead_code)] annotations remain in the codebase"
    - "Zero _for_test() wrapper functions remain"
    - "All 13 tests pass using direct pub(crate) function calls"
    - "cargo clippy --all-targets -- -D warnings passes clean"
    - "cargo fmt --all -- --check passes clean"
  artifacts:
    - path: "src/app.rs"
      provides: "App struct with db_path and article_list_offset fields removed"
    - path: "src/db.rs"
      provides: "Data types without #[allow(dead_code)] annotations"
    - path: "src/sync.rs"
      provides: "pub(crate) parse functions, no _for_test wrappers"
    - path: "src/ui.rs"
      provides: "pub(crate) helper functions, no _for_test wrappers"
    - path: "src/main.rs"
      provides: "Tests updated to call pub(crate) functions directly"
  key_links:
    - from: "src/main.rs tests"
      to: "src/sync.rs parse_day_page"
      via: "crate::sync::parse_day_page (pub(crate))"
      pattern: "crate::sync::parse_day_page\\("
    - from: "src/main.rs tests"
      to: "src/ui.rs month_name"
      via: "crate::ui::month_name (pub(crate))"
      pattern: "crate::ui::month_name\\("
---

<objective>
Remove all dead code and eliminate test wrapper anti-patterns.

Purpose: Clean up 9 `#[allow(dead_code)]` annotations and 4 `_for_test()` wrapper functions before module decomposition. These are low-risk, high-confidence changes that reduce noise and establish the correct visibility (`pub(crate)`) that decomposition will rely on.

Output: All dead fields removed, all test wrappers deleted, functions made `pub(crate)`, all 13 tests updated to call functions directly.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/REFACTOR.md

@src/app.rs
@src/db.rs
@src/sync.rs
@src/ui.rs
@src/main.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove dead code — unused fields and annotations</name>
  <files>src/app.rs, src/db.rs, src/sync.rs</files>
  <behavior>
    - All 13 existing tests pass after changes
    - cargo clippy --all-targets -- -D warnings produces zero warnings
    - rg '#\[allow\(dead_code\)\]' src/ returns zero matches
  </behavior>
  <action>
Remove dead code in three files:

**src/app.rs:**
1. Delete `db_path: PathBuf` field (line ~57-81). Remove from struct definition, remove from `App::new()` parameter list and struct literal. Update `App::new` signature to remove `db_path` parameter. Update call site in `src/main.rs` where `App::new` is called — remove the `db_path` argument.
2. Delete `article_list_offset: usize` field (line ~89). Remove from struct definition and `App::new()` struct literal (set to `0` currently).
3. Remove `#[allow(dead_code)]` from `ImageState` struct (line ~57). This struct IS used — `ImageLoadState::Loaded(Box<ImageState>)` — the annotation is unnecessary.

**src/db.rs:**
4. Remove `#[allow(dead_code)]` from `Article` struct (line 9). Fields are read in tests and in `ui.rs` rendering.
5. Remove `#[allow(dead_code)]` from `FullArticle` struct (line 18). Fields are read in article reader rendering.
6. Remove `#[allow(dead_code)]` from `ArticleImage` struct (line 29). Fields are read in image loading.
7. Remove `#[allow(dead_code)]` from `Db::get_pending_dates()` (line 210). This method is defined but has the dead_code annotation — it is NOT called anywhere in the codebase. Keep the method but remove the annotation, and add `#[allow(dead_code)]` annotation removal. If clippy then complains, delete the entire method since it's unused. Actually — just remove the annotation. If the method is unused, clippy will warn. Then delete the method. Check: `rg 'get_pending_dates' src/` — if only the definition matches, delete the method.

**src/sync.rs:**
8. Remove `#[allow(dead_code)]` from `ArticleLink::section` field (line ~361). The field is written in struct construction. After removal, if clippy warns, keep the field but note that it's used in construction even if not individually read.
  </action>
  <verify>
    <automated>cd /home/simon/coding/lapresse-tui && cargo test 2>&1 && cargo clippy --all-targets -- -D warnings 2>&1 && rg '#\[allow\(dead_code\)\]' src/ && echo "FAIL: dead_code annotations still exist" || echo "PASS: no dead_code annotations"</automated>
  </verify>
  <done>
- Zero `#[allow(dead_code)]` annotations in codebase
- `App::db_path` field removed
- `App::article_list_offset` field removed
- `Db::get_pending_dates()` removed (if unused) or annotation removed
- All 13 tests pass
- Clippy clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Eliminate _for_test() wrappers — make functions pub(crate)</name>
  <files>src/sync.rs, src/ui.rs, src/main.rs</files>
  <behavior>
    - All 13 tests pass using direct pub(crate) function calls
    - rg '_for_test' src/ returns zero matches
    - Test for parse_day_page calls crate::sync::parse_day_page() and destructures ArticleLink directly
    - Test for parse_article_page calls crate::sync::parse_article_page() and destructures ParsedArticle directly
    - Test for month_name calls crate::ui::month_name()
    - Test for format_scroll_indicator calls crate::ui::format_scroll_indicator()
  </behavior>
  <action>
Eliminate all 4 `_for_test()` wrappers by making underlying functions `pub(crate)` and updating test call sites.

**src/sync.rs:**
1. Change `fn parse_day_page(html: &str) -> Result<Vec<ArticleLink>>` to `pub(crate) fn parse_day_page(html: &str) -> Result<Vec<ArticleLink>>`
2. Change `fn parse_article_page(html: &str, article_url: &str) -> Result<ParsedArticle>` to `pub(crate) fn parse_article_page(html: &str, article_url: &str) -> Result<ParsedArticle>`
3. Delete the entire `#[cfg(test)]` block containing `parse_day_page_for_test` (lines ~701-708) and `parse_article_page_for_test` (lines ~710-733)
4. Make `ArticleLink` and `ParsedArticle` structs `pub(crate)` so tests can access their fields

**src/ui.rs:**
5. Change `fn month_name(month: u8) -> &'static str` to `pub(crate) fn month_name(month: u8) -> &'static str`
6. Change `fn format_scroll_indicator(pct: u32, scrollable: bool) -> String` to `pub(crate) fn format_scroll_indicator(pct: u32, scrollable: bool) -> String`
7. Delete the `#[cfg(test)]` block containing `month_name_for_test` and `format_scroll_indicator_for_test` (lines ~1134-1142)

**src/main.rs — update test call sites:**
8. Update `sync_parse_day_page` test: Replace `crate::sync::parse_day_page_for_test(html).unwrap()` with `crate::sync::parse_day_page(html).unwrap()`. Update assertions to use `.url`, `.title`, `.time` fields on `ArticleLink` instead of tuple indexing `.0`, `.1`, `.2`.
9. Update `sync_parse_article_page` test: Replace `crate::sync::parse_article_page_for_test(html, "https://lapresse.ca/test").unwrap()` with `crate::sync::parse_article_page(html, "https://lapresse.ca/test").unwrap()`. Update assertions to use `ParsedArticle` struct fields (`.title`, `.section`, `.author`, `.published_at`, `.content_text`, `.images`) instead of tuple indexing.
10. Update `sync_parse_empty_day_page` test: Replace `crate::sync::parse_day_page_for_test(html).unwrap()` with `crate::sync::parse_day_page(html).unwrap()`.
11. Update `ui_month_name` test: Replace `crate::ui::month_name_for_test` with `crate::ui::month_name`.
12. Update `ui_format_scroll_indicator` test: Replace `crate::ui::format_scroll_indicator_for_test` with `crate::ui::format_scroll_indicator`.
  </action>
  <verify>
    <automated>cd /home/simon/coding/lapresse-tui && cargo test 2>&1 && cargo clippy --all-targets -- -D warnings 2>&1 && rg '_for_test' src/ && echo "FAIL: _for_test wrappers still exist" || echo "PASS: no _for_test wrappers"</automated>
  </verify>
  <done>
- All 4 `_for_test()` wrapper functions deleted
- `parse_day_page`, `parse_article_page`, `month_name`, `format_scroll_indicator` are `pub(crate)`
- `ArticleLink` and `ParsedArticle` structs are `pub(crate)` with visible fields
- All 13 tests updated to call functions directly
- All 13 tests pass
- Clippy clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure internal refactoring — no trust boundary changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | N/A | N/A | accept | No security-relevant changes in dead code cleanup |
</threat_model>

<verification>
1. `cargo test` — all 13 tests pass
2. `cargo clippy --all-targets -- -D warnings` — zero warnings
3. `cargo fmt --all -- --check` — formatted
4. `rg '#\[allow\(dead_code\)\]' src/` — zero matches
5. `rg '_for_test' src/` — zero matches
</verification>

<success_criteria>
- Zero `#[allow(dead_code)]` annotations remain
- Zero `_for_test()` wrapper functions remain
- `App::db_path` and `App::article_list_offset` fields removed
- `Db::get_pending_dates()` removed (confirmed unused)
- All 13 tests pass with direct `pub(crate)` function calls
- Clippy and fmt clean
</success_criteria>

<output>
After completion, create `.planning/phases/99-refactor/99-01-SUMMARY.md`
</output>
