---
phase: 99-refactor
plan: 07
type: execute
wave: 4
depends_on: ["99-03", "99-04", "99-05", "99-06"]
files_modified:
  - src/main.rs
  - src/db/mod.rs
  - src/sync/mod.rs
  - src/ui/mod.rs
autonomous: true
requirements: [REFACTOR-08]

must_haves:
  truths:
    - "All 13 tests moved from main.rs into per-module #[cfg(test)] mod tests blocks"
    - "main.rs reduced to ~120 lines (CLI + event loop only)"
    - "Each test lives in the module it tests"
    - "All 13 tests pass"
    - "cargo clippy passes clean"
  artifacts:
    - path: "src/db/mod.rs"
      provides: "7 DB tests in #[cfg(test)] mod tests"
      contains: "#[cfg(test)] mod tests"
    - path: "src/sync/mod.rs"
      provides: "3 sync tests in #[cfg(test)] mod tests"
      contains: "#[cfg(test)] mod tests"
    - path: "src/ui/mod.rs"
      provides: "2 UI tests in #[cfg(test)] mod tests"
      contains: "#[cfg(test)] mod tests"
    - path: "src/main.rs"
      provides: "CLI + event loop only, no test module"
      max_lines: 160
  key_links:
    - from: "src/db/mod.rs tests"
      to: "src/db/mod.rs Db methods"
      via: "inline — same module, private access"
      pattern: "fn test_db\\(\\)"
    - from: "src/sync/mod.rs tests"
      to: "src/sync/scraping.rs"
      via: "super::scraping::parse_day_page"
      pattern: "parse_day_page"
    - from: "src/ui/mod.rs tests"
      to: "src/ui/mod.rs helpers"
      via: "super::month_name"
      pattern: "super::month_name"
---

<objective>
Move all 13 tests from main.rs into per-module test blocks.

Purpose: Tests should live next to the code they test. After decomposition, each module should own its tests. This reduces main.rs from ~460 lines to ~130 lines and makes the test coverage visible per module.

Output: `src/main.rs` reduced to CLI + event loop. 7 DB tests in `db/mod.rs`, 3 sync tests in `sync/mod.rs`, 2 UI tests in `ui/mod.rs`. All 13 tests pass.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/REFACTOR.md

@src/main.rs
@src/db/mod.rs
@src/sync/mod.rs
@src/ui/mod.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move tests into per-module test blocks</name>
  <files>src/main.rs, src/db/mod.rs, src/sync/mod.rs, src/ui/mod.rs</files>
  <behavior>
    - All 13 tests pass from new locations
    - cargo test output shows tests namespaced by module
    - main.rs has no #[cfg(test)] mod tests block
    - Each module has its own #[cfg(test)] mod tests
  </behavior>
  <action>
Move 13 tests from `src/main.rs` `mod tests` block into their respective modules.

**Test inventory and destination mapping:**

| Test | Tests What | Destination |
|------|-----------|-------------|
| `db_open_creates_schema` | Db::open | `src/db/mod.rs` |
| `db_insert_and_retrieve_article` | Db::insert_article, get_full_article | `src/db/mod.rs` |
| `db_insert_article_idempotent` | Db::insert_article OR IGNORE | `src/db/mod.rs` |
| `db_articles_by_date` | Db::articles_by_date | `src/db/mod.rs` |
| `db_article_counts_by_month` | Db::article_counts_by_month | `src/db/mod.rs` |
| `db_insert_image` | Db::insert_image, get_full_article images | `src/db/mod.rs` |
| `db_sync_state` | Db::upsert_sync_state, get_sync_state | `src/db/mod.rs` |
| `db_search_articles` | Db::search_articles | `src/db/mod.rs` |
| `sync_parse_day_page` | parse_day_page | `src/sync/mod.rs` or `src/sync/scraping.rs` |
| `sync_parse_article_page` | parse_article_page | `src/sync/mod.rs` or `src/sync/scraping.rs` |
| `sync_parse_empty_day_page` | parse_day_page empty | `src/sync/mod.rs` or `src/sync/scraping.rs` |
| `ui_month_name` | month_name | `src/ui/mod.rs` |
| `ui_format_scroll_indicator` | format_scroll_indicator | `src/ui/mod.rs` |

**Step-by-step:**

1. **Move DB tests (7) to `src/db/mod.rs`:**
   - Add at bottom of file:
   ```rust
   #[cfg(test)]
   mod tests {
       use super::*;
       use crate::db::types::*;
       
       fn test_db() -> Db {
           Db::open(std::path::Path::new(":memory:")).unwrap()
       }
       
       // ... paste all 7 db_* tests here
   }
   ```
   - Update imports: tests use `NewArticle`, `NewImage` — these are now in `types` module, accessible via `super::*` (since mod.rs has `use types::*`) or via explicit `crate::db::types::*`

2. **Move sync tests (3) to `src/sync/mod.rs`:**
   - Add at bottom of file:
   ```rust
   #[cfg(test)]
   mod tests {
       use super::*;
       
       // ... paste sync_parse_day_page, sync_parse_article_page, sync_parse_empty_day_page
   }
   ```
   - Tests call `crate::sync::parse_day_page(html)` — via re-export this resolves to `super::scraping::parse_day_page`. Or use `super::parse_day_page()` since mod.rs re-exports it.
   - Tests access `ArticleLink` fields (`.url`, `.title`, `.time`) — re-exported in mod.rs ✓
   - Tests access `ParsedArticle` fields — re-exported in mod.rs ✓

3. **Move UI tests (2) to `src/ui/mod.rs`:**
   - Add at bottom of file:
   ```rust
   #[cfg(test)]
   mod tests {
       use super::*;
       
       // ... paste ui_month_name, ui_format_scroll_indicator
   }
   ```
   - Tests call `month_name()` and `format_scroll_indicator()` — accessible via `super::*` ✓

4. **Delete entire `mod tests` block from `src/main.rs`:**
   - Remove lines `#[cfg(test)] mod tests { ... }` (everything from line ~155 to end of file)
   - Remove `use crate::db::{Db, NewArticle, NewImage};` if it was only used by tests
   - main.rs should now be ~155 lines (CLI entry + event loop)

5. **Verify each test still works:**
   - Run `cargo test` — all 13 must pass
   - Check test output shows module namespacing: `db::tests::db_open_creates_schema`, etc.
  </action>
  <verify>
    <automated>cd /home/simon/coding/lapresse-tui && cargo test 2>&1 && cargo clippy --all-targets -- -D warnings 2>&1</automated>
  </verify>
  <done>
- 7 DB tests in `src/db/mod.rs` `#[cfg(test)] mod tests`
- 3 sync tests in `src/sync/mod.rs` `#[cfg(test)] mod tests`
- 2 UI tests in `src/ui/mod.rs` `#[cfg(test)] mod tests`
- `src/main.rs` has no test module
- main.rs is ~130-155 lines
- All 13 tests pass
- Clippy clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test reorganization only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-07 | N/A | N/A | accept | Test relocation has no security implications |
</threat_model>

<verification>
1. `cargo test` — all 13 tests pass
2. `cargo clippy --all-targets -- -D warnings` — zero warnings
3. `cargo fmt --all -- --check` — formatted
4. `rg '#\[cfg\(test\)\]' src/main.rs && echo "FAIL: tests still in main.rs" || echo "PASS"`
5. `wc -l src/main.rs` — should be ~130-155 lines
6. `cargo test 2>&1 | grep 'test ' | wc -l` — should show 13 tests
</verification>

<success_criteria>
- All 13 tests moved to per-module test blocks
- main.rs reduced to CLI + event loop (~130-155 lines)
- Each module has its own `#[cfg(test)] mod tests`
- All 13 tests pass
- Clippy and fmt clean
</success_criteria>

<output>
After completion, create `.planning/phases/99-refactor/99-07-SUMMARY.md`
</output>
