---
phase: 99-refactor
plan: 05
type: execute
wave: 3
depends_on: ["99-01"]
files_modified:
  - src/ui.rs
  - src/ui/mod.rs
  - src/ui/calendar.rs
  - src/ui/article_list.rs
  - src/ui/article_reader.rs
  - src/ui/search.rs
  - src/ui/help.rs
autonomous: true
requirements: [REFACTOR-06]

must_haves:
  truths:
    - "ui.rs decomposed into ui/ module directory with mod.rs and 5 view submodules"
    - "External API surface unchanged — crate::ui::render() still works"
    - "All 13 tests pass"
    - "cargo clippy passes clean"
  artifacts:
    - path: "src/ui/mod.rs"
      provides: "render() dispatcher, shared helpers"
      exports: ["render", "month_name", "format_scroll_indicator"]
    - path: "src/ui/calendar.rs"
      provides: "Calendar view rendering"
      min_lines: 150
    - path: "src/ui/article_list.rs"
      provides: "Article list view rendering"
      min_lines: 80
    - path: "src/ui/article_reader.rs"
      provides: "ContentBlock enum, article reader with virtual scrolling"
      min_lines: 200
    - path: "src/ui/search.rs"
      provides: "Search input and results rendering"
      min_lines: 80
    - path: "src/ui/help.rs"
      provides: "Help overlay rendering"
      min_lines: 40
  key_links:
    - from: "src/ui/mod.rs"
      to: "src/ui/calendar.rs"
      via: "mod calendar; use calendar::render_calendar"
      pattern: "mod calendar"
    - from: "src/main.rs"
      to: "src/ui/mod.rs"
      via: "crate::ui::render"
      pattern: "crate::ui::render"
---

<objective>
Decompose ui.rs (1,142 lines) into a ui/ module directory.

Purpose: Split the largest file into view-specific submodules. Each render function maps to a Focus enum variant, making the decomposition natural. The `ContentBlock` enum and virtual scrolling logic belong with the article reader.

Output: `src/ui.rs` deleted, replaced by `src/ui/mod.rs` + 5 view submodules. All external imports preserved via `pub use` re-exports.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/REFACTOR.md

@src/ui.rs
@src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ui module directory with view submodules</name>
  <files>src/ui/mod.rs, src/ui/calendar.rs, src/ui/article_list.rs, src/ui/article_reader.rs, src/ui/search.rs, src/ui/help.rs</files>
  <action>
Decompose `src/ui.rs` (1,142 lines) into `src/ui/` module directory.

**Current structure of ui.rs:**
- Lines ~1-30: Imports
- Lines ~30-80: ContentBlock enum and related helpers
- Lines ~80-300: `render()` — main dispatcher (match on Focus enum)
- Lines ~300-550: Calendar rendering (`render_calendar`, date highlighting)
- Lines ~550-700: Article list rendering
- Lines ~700-900: Article reader rendering (virtual scroll, image rendering)
- Lines ~900-1050: Search rendering
- Lines ~1050-1115: Help overlay rendering
- Lines ~1115-1142: `month_name()` and `format_scroll_indicator()` helpers (now `pub(crate)`, wrappers deleted)

**Exact decomposition — read the actual function boundaries from ui.rs and split:**

1. Create directory `src/ui/`

2. Create `src/ui/article_reader.rs` — move into it:
   - `ContentBlock` enum (Text/Image variant)
   - `render_article_reader()` function
   - Virtual scrolling logic, image rendering with `ratatui-image`
   - All helper functions specific to article reader
   - Make `ContentBlock` `pub(crate)` if it's used by App
   - Imports: `ratatui::*`, `ratatui_image::*`, `crate::app::*`, `crate::db::*`

3. Create `src/ui/calendar.rs` — move into it:
   - `render_calendar()` function
   - Calendar grid rendering, date highlighting, month navigation display
   - Imports: `ratatui::*`, `crate::app::*`, `crate::db::*`

4. Create `src/ui/article_list.rs` — move into it:
   - Article list rendering function
   - Section filtering display, article metadata formatting
   - Imports: `ratatui::*`, `crate::app::*`, `crate::db::*`

5. Create `src/ui/search.rs` — move into it:
   - Search input rendering
   - Search results list rendering
   - Imports: `ratatui::*`, `crate::app::*`

6. Create `src/ui/help.rs` — move into it:
   - Help overlay rendering
   - Keybinding display
   - Imports: `ratatui::*`

7. Create `src/ui/mod.rs`:
   ```
   mod article_list;
   mod article_reader;
   mod calendar;
   mod help;
   mod search;
   
   pub(crate) use article_reader::ContentBlock;
   pub(crate) use calendar::render_calendar;
   pub(crate) use article_list::render_article_list;
   pub(crate) use article_reader::render_article_reader;
   pub(crate) use search::render_search;
   pub(crate) use help::render_help;
   
   use ratatui::*;
   use crate::app::App;
   
   pub fn render(f: &mut Frame, app: &App) {
       // Main dispatcher — match on app.focus
       // Calls submodule render functions
   }
   
   pub(crate) fn month_name(month: u8) -> &'static str { ... }
   pub(crate) fn format_scroll_indicator(pct: u32, scrollable: bool) -> String { ... }
   ```
   - `render()` stays in `mod.rs` — it's the public entry point that dispatches to submodules
   - `month_name()` and `format_scroll_indicator()` stay in `mod.rs` — they're shared helpers
   - The `render()` function calls into the submodule functions

8. Delete `src/ui.rs`

9. **Each submodule function needs `App` reference.** All render functions receive `&App` as a parameter. The submodule functions should take `(f: &mut Frame, app: &App, area: Rect)` — same pattern as current inline code, just extracted.

10. **No circular dependencies.** UI submodules only READ from App, never write. Dependency: `ui/*` → `app` (read App state), `ui/*` → `db` (read types).
  </action>
  <verify>
    <automated>cd /home/simon/coding/lapresse-tui && cargo test 2>&1 && cargo clippy --all-targets -- -D warnings 2>&1</automated>
  </verify>
  <done>
- `src/ui.rs` deleted
- `src/ui/mod.rs` created with `render()` dispatcher + shared helpers + `pub use` re-exports
- `src/ui/calendar.rs` — calendar view
- `src/ui/article_list.rs` — article list view
- `src/ui/article_reader.rs` — ContentBlock + article reader
- `src/ui/search.rs` — search view
- `src/ui/help.rs` — help overlay
- All 13 tests pass
- Clippy clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Internal module reorganization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-05 | N/A | N/A | accept | Module decomposition has no security implications |
</threat_model>

<verification>
1. `cargo test` — all 13 tests pass
2. `cargo clippy --all-targets -- -D warnings` — zero warnings
3. `cargo fmt --all -- --check` — formatted
4. `test -f src/ui.rs && echo "FAIL" || echo "PASS"`
5. `ls src/ui/mod.rs src/ui/calendar.rs src/ui/article_list.rs src/ui/article_reader.rs src/ui/search.rs src/ui/help.rs`
</verification>

<success_criteria>
- `ui.rs` decomposed into `ui/` module with 6 files
- External API unchanged via re-exports
- All 13 tests pass
- Clippy and fmt clean
</success_criteria>

<output>
After completion, create `.planning/phases/99-refactor/99-05-SUMMARY.md`
</output>
