---
phase: 99-refactor
plan: 04
type: execute
wave: 3
depends_on: ["99-01"]
files_modified:
  - src/db.rs
  - src/db/mod.rs
  - src/db/types.rs
autonomous: true
requirements: [REFACTOR-05]

must_haves:
  truths:
    - "db.rs decomposed into db/ module directory with mod.rs and types.rs"
    - "External API surface unchanged — crate::db::* still resolves via pub use re-exports"
    - "All 13 tests pass"
    - "cargo clippy passes clean"
  artifacts:
    - path: "src/db/mod.rs"
      provides: "Db struct and all query methods"
      exports: ["Db", "article_count", "articles_by_date", "insert_article", "insert_image", "search_articles", "get_full_article"]
    - path: "src/db/types.rs"
      provides: "Article, FullArticle, ArticleImage, NewArticle, NewImage"
      min_lines: 50
  key_links:
    - from: "src/db/mod.rs"
      to: "src/db/types.rs"
      via: "pub use types::*"
      pattern: "pub use types::"
    - from: "src/main.rs"
      to: "src/db/mod.rs"
      via: "crate::db::{Db, NewArticle, NewImage}"
      pattern: "crate::db::"
---

<objective>
Decompose db.rs (298 lines) into a db/ module directory.

Purpose: Split data types (Article, FullArticle, etc.) from the Db implementation. This is the simplest decomposition — db.rs is already well-scoped.

Output: `src/db.rs` deleted, replaced by `src/db/mod.rs` (Db struct + methods) and `src/db/types.rs` (data structs).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/REFACTOR.md

@src/db.rs
@src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create db module directory with types and methods</name>
  <files>src/db/mod.rs, src/db/types.rs</files>
  <action>
Decompose `src/db.rs` into `src/db/` module directory.

**Step-by-step:**

1. Create directory `src/db/`

2. Create `src/db/types.rs` — move into it:
   - `Article` struct
   - `FullArticle` struct
   - `ArticleImage` struct
   - `NewArticle` struct
   - `NewImage` struct
   - All structs should be `pub` with `pub` fields as they currently are
   - No imports needed beyond what's already there (just field types: `String`, `Option`, `Vec`, `u32`, lifetimes on `NewArticle`/`NewImage`)

3. Create `src/db/mod.rs`:
   ```
   mod types;
   
   pub use types::{Article, ArticleImage, FullArticle, NewArticle, NewImage};
   
   use anyhow::Result;
   use rusqlite::Connection;
   use std::collections::HashMap;
   use std::path::Path;
   use std::sync::Mutex;
   use types::*;
   
   const SCHEMA_SQL: &str = include_str!("../../migrations/V1__initial_schema.sql");
   
   pub struct Db { ... }
   impl Db { ... }
   ```
   - Move the `Db` struct, `SCHEMA_SQL` constant, and all `impl Db` methods into `mod.rs`
   - Import `types::*` locally for use in method bodies (query_map closures construct these types)
   - The `include_str!` path changes from `../migrations/` to `../../migrations/` since mod.rs is one level deeper

4. Delete `src/db.rs`

5. **Verify no import breakage:** The key consumers are:
   - `src/main.rs`: `use crate::db::{Db, NewArticle, NewImage}` — works via re-export ✓
   - `src/app.rs`: `use crate::db::{Article, Db, FullArticle}` — works via re-export ✓
   - `src/sync/` (after Plan 03): `use crate::db::{Db, NewArticle, NewImage}` — works via re-export ✓
   - `src/ui.rs`: uses `crate::db::*` types — works via re-export ✓
  </action>
  <verify>
    <automated>cd /home/simon/coding/lapresse-tui && cargo test 2>&1 && cargo clippy --all-targets -- -D warnings 2>&1</automated>
  </verify>
  <done>
- `src/db.rs` deleted
- `src/db/mod.rs` contains Db struct + all methods + SCHEMA_SQL
- `src/db/types.rs` contains all 5 data structs
- `pub use types::*` re-exports preserve external API
- `include_str!` path updated to `../../migrations/`
- All 13 tests pass
- Clippy clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Internal module reorganization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-04 | N/A | N/A | accept | Module decomposition has no security implications |
</threat_model>

<verification>
1. `cargo test` — all 13 tests pass
2. `cargo clippy --all-targets -- -D warnings` — zero warnings
3. `cargo fmt --all -- --check` — formatted
4. `test -f src/db.rs && echo "FAIL: old file still exists" || echo "PASS: old file removed"`
5. `test -f src/db/mod.rs && echo "PASS" || echo "FAIL"`
6. `test -f src/db/types.rs && echo "PASS" || echo "FAIL"`
7. `rg 'pub use' src/db/mod.rs` — re-exports present
</verification>

<success_criteria>
- `db.rs` decomposed into `db/` module with 2 files
- External API unchanged via `pub use` re-exports
- All 13 tests pass
- Clippy and fmt clean
</success_criteria>

<output>
After completion, create `.planning/phases/99-refactor/99-04-SUMMARY.md`
</output>
